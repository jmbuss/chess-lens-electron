/**
 * Base IPC channel definitions
 * This interface can be augmented by each service to add type-safe channels
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface IpcChannels {
  // This will be augmented by individual handlers
}
